machine:
  services:
    - docker

  timezone:
    Asia/Tokyo

  ruby:
    version:
      2.2.0

  hosts:
    circlehost: 127.0.0.1

dependencies:
  pre:
    # - npm install coffeescript # install from a different package manager
    - gem uninstall bundler # use a custom version of bundler
    - gem install bundler -v 1.10.5

  override:
    - docker info
    # - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    - docker build -t violetyk/rails-sample .
    # - mkdir -p ~/docker; docker save violetyk/rails-sample > ~/docker/image.tar
    # - sudo pip install awscli
    # - bundle update: # note ':' here
        # timeout: 180 # fail if command has no output for 3 minutes

  # we automatically cache and restore many dependencies between
  # builds. If you need to, you can add custom paths to cache:
  # cache_directories:
    # - "~/docker"

database:
  override:
    - cp config/database.yml.ci config/database.yml
    # - bundle exec rake db:schema:load RAILS_ENV=test

test:
  override:
    - docker run -d -p 3000:3000 violetyk/rails-sample bundle exec unicorn -c config/unicorn.rb -D -E production; sleep 10
    - curl --retry 10 --retry-delay 5 -v http://localhost:3000

## Customize deployment commands
deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push violetyk/rails-sample
  # staging:
    # branch: master
    # heroku:
      # appname: foo-bar-123

## Custom notifications
# notify:
  # webhooks:
    # # A list of hashes representing hooks. Only the url field is supported.
    # - url: https://someurl.com/hooks/circle
